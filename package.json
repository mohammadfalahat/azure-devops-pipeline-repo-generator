{
  "name": "azure-devops-pipeline-repo-generator",
  "version": "1.0.0",
  "description": "Azure DevOps extension that adds a generate pipeline action next to each branch.",
  "main": "index.js",
  "scripts": {
    "build": "echo \"Static extension: no build step required\"",
    "test": "node scripts/validate-extension.js && node scripts/validate-action-behavior.js && node scripts/validate-ui-behavior.js && node scripts/service-hook-listener.js --self-test && bash scripts/provision-pipeline-release.sh --self-test",
    "service-hook:listen": "node scripts/service-hook-listener.js",
    "pipeline:provision-release": "bash scripts/provision-pipeline-release.sh",
    "extension:package": "npx --yes tfx-cli extension create --manifest-globs vss-extension.json --rev-version",
    "extension:publish": "npx --yes tfx-cli extension publish --service-url \"$ADO_SERVICE_URL\" --token \"$AZP_TOKEN\" --vsix"
  },
  "keywords": [
    "azure-devops",
    "extension",
    "pipelines",
    "repositories"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "vss-web-extension-sdk": "^5.141.0"
  }
}
